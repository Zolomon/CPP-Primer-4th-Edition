#include <iostream>

double calc() {
	
	return 0.0;
}

int main() {
	// Commented lines are errors.
	//int car = 1024, auto = 2048; // auto is keyword
	int ival = ival;
	//std::cin >> int input_value; // input_value needs to be declared before use.
	//double salary = wage = 9999.99; // wage needs to be declared before use.
	//double calc = calc();

	std::cout << ival << std::endl;

	return 0;
}