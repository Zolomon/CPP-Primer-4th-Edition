/*

lvalue = Expressions that may appear as either the left-hand or right-hand side of an assignment. Have an address.

rvalue = Expressions that may appear on the right-hand but not left-hand side of an assignment. Does not have an address.

Examples: 
int units_sold = 0; // units_sold = lvalue, 0 = rvalue

*/