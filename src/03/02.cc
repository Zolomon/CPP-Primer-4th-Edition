/*

A default constructor is the constructor that is used by default when no parameters are specified. 

For example: 

class Example
{
public:
	Example();
};

Here, We can instantiate our type on the stack with Example myExample; and it will be equivalent to Example myExample();.

*/