/*

input operator for strings: 
Leading whitespace is discarded. 
Then everything is read until next whitespace occurs, whitespace non-including.

getline: 
Leading new lines are not discarded. 
Then everything is read until next newline character, newline character non-including.

*/